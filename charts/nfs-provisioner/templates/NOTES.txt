Thank you for installing {{ .Chart.Name }}!

Your NFS provisioner has been deployed to the {{ .Release.Namespace }} namespace.

Configuration:
  NFS Server: {{ .Values.nfs.server }}
  NFS Path: {{ .Values.nfs.path }}
  Storage Class: {{ .Values.storageClass.name }}
  Default Class: {{ .Values.storageClass.defaultClass }}

To verify the deployment is running:
  kubectl get pods -n {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "nfs-provisioner.name" . }},app.kubernetes.io/instance={{ .Release.Name }}"

To verify the storage class was created:
  kubectl get storageclass {{ .Values.storageClass.name }}

IMPORTANT: Make sure you have added all cluster node IP addresses to your UNAS Pro.
This can be done in the web UI of the drive under:
  Settings > Services > Add NFS connections

To test the provisioner, try installing HashiCorp Vault:
  helm repo add hashicorp https://helm.releases.hashicorp.com
  helm install vault hashicorp/vault
